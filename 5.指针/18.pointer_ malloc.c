# include <stdio.h>

void f(void)
{

}
    

/**
 * 动态内存分配
 *      传统数组的缺点
 *          1.数组的长度必须事先制定，并且这个长度只能是长整数，不能是变量。
 *          2.传统形式定义的数组，该数组的内存程序员无法手动释放。
 *      为什么需要动态内存分配
 *      动态内存分配举例：动态数组的构造
 *      静态内存和动态内存的比较
 *      跨函数使用内存的问题
*/

int main(void)
{
    f();

    return 0;
}